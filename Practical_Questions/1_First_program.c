#include <stdio.h>

int main() {
    // Declaring variables for name and age
    char name[] = "John Doe";
    int age = 30;

    // Printing name and age using printf
    printf("Name: %s\n", name);
    printf("Age: %d\n", age);

    return 0;
}